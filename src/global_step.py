# pipeline(step, step_stats, params).first(initialize)
# pipeline(step, step_stats, params).last(finalize)
# pipeline(step, step_stats, params).eq(2000, checkpoint2k)
# pipeline(step, step_stats, params).mod(50, doSomething)
